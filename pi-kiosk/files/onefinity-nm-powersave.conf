# Wi-Fi power save off.
#
# The radio naps between packets, which adds latency spikes and drops. On a
# machine that is driven from a phone over remote access, and that reports job
# progress, an unreliable link is worse than the milliwatts are worth.
[connection]
wifi.powersave = 2

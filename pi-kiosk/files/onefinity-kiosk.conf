# Onefinity kiosk settings. Reboot (or: sudo systemctl restart onefinity-kiosk)
# after changing.

# Page shown full-screen.
KIOSK_URL=http://localhost:4000

# Screen rotation for a portrait touch monitor: normal, 90, 180 or 270.
KIOSK_ROTATE=normal
